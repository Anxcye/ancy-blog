# Frontend Admin

Admin workspace for blog content, translation operations, and publish controls.

## Run

```bash
cp .env.example .env
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Env

Use `VITE_API_BASE_URL` to point to backend API base, e.g. `http://localhost:8080/api/v1`.
